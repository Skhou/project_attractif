{% extends "IpAttractifBundle::layout.html.twig" %}
{% block content_header %}{{ typeOfAction }} de lieu{% endblock %}
{% block body %}
    <div class="row">

        {% for flashMessage in app.session.flashbag.get('notice') %}
            <div class="flash-notice">
                {{ flashMessage }}
                <br><br>
            </div>
        {% endfor %}

        <!-- general form elements -->
        <div>
            <div class="col-md-6">
                <!-- form start -->
                {{ form_start(form, {attr: {role: 'form'}}) }}
                <div class="box-body">
                    <div class="form-group">
                        {{ form_label(form.name, 'Nom du lieu') }}
                        {{ form_widget(form.name, {attr: {class: 'form-control'}}) }}
                        {{ form_errors(form.name) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.address, 'Adresse') }}
                        {{ form_widget(form.address, {attr: {class: 'form-control'}}) }}
                        {{ form_errors(form.address) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.zipcode, 'Code postal') }}
                        {{ form_widget(form.zipcode, {attr: {class: 'form-control'}}) }}
                        {{ form_errors(form.zipcode) }}
                    </div>
                    <div class="form-group">
                        {{ form_label(form.city, 'Ville') }}
                        {{ form_widget(form.city, {attr: {class: 'form-control'}}) }}
                        {{ form_errors(form.city) }}
                    </div>
                </div>
                <div class="box-footer">
                    <button type="submit" class="btn btn-primary">{{ bouton }}</button>
                </div>
                {{ form_end(form) }}
            </div>
        </div><!-- /.box -->
    </div>
{% endblock %}